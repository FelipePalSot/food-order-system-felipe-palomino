apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: catalog-service
  labels:
    app: catalog-service
spec:
  type: NodePort
  selector:
    app: catalog-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8082
      nodePort: 30082

